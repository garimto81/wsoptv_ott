# WSOPTV 멀티노드 역할 분리

**생성일**: 2026-02-04

---

## 노드 역할 정의

| 노드 | 역할명 | 담당 영역 | 상태 파일 |
|:----:|--------|----------|----------|
| **Node A** | 📝 Docs Node | 문서, 기획, 전략 | `.omc/nodes/docs-state.json` |
| **Node B** | ⚙️ Sync Node | 스크립트, 동기화, 자동화 | `.omc/nodes/sync-state.json` |

---

## 세션 시작 프로토콜

### 1. 노드 역할 선언 (세션 시작 시 필수)

```
/auto 이 세션은 [Docs Node / Sync Node] 입니다
```

또는 자동 감지:
- `docs/` 폴더 작업 시작 → Docs Node로 간주
- `scripts/` 폴더 작업 시작 → Sync Node로 간주

### 2. 충돌 확인

세션 시작 시 다른 노드 상태 확인:

```bash
# 다른 노드가 활성 상태인지 확인
cat .omc/nodes/docs-state.json   # Docs Node 상태
cat .omc/nodes/sync-state.json   # Sync Node 상태
```

`"active": true`인 경우 해당 영역 작업 피하기

---

## 담당 영역 상세

### 📝 Docs Node (문서/기획)

**수정 가능**:
- `docs/prds/**` - PRD 문서
- `docs/strategies/**` - 전략 문서
- `docs/mockups/**` - HTML 목업
- `docs/images/**` - 스크린샷
- `docs/adrs/**` - 아키텍처 결정
- `.omc/plans/**` - 작업 계획
- `CLAUDE.md`, `README.md`

**읽기만 가능** (수정 금지):
- `scripts/**` - Sync Node 담당
- `docs/management/**` - Sync Node 담당 (업체 데이터)

### ⚙️ Sync Node (동기화/자동화)

**수정 가능**:
- `scripts/**` - 모든 스크립트
- `docs/management/**` - 업체 관리 데이터
  - `VENDOR-DASHBOARD.md`
  - `EMAIL-LOG.md`
  - `SLACK-LOG.md`
  - `.sync_state.json`
  - `.slacklist_state.json`

**읽기만 가능** (수정 금지):
- `docs/prds/**` - Docs Node 담당
- `docs/strategies/**` - Docs Node 담당

---

## 공유 영역 (조율 필요)

다음 파일은 **양쪽 노드 모두 수정 가능**하지만, 수정 전 조율 필요:

| 파일 | 조율 방법 |
|------|----------|
| `.omc/ralplan-state.json` | 한 번에 한 노드만 ralplan 실행 |
| `.omc/ultrawork-state.json` | 한 번에 한 노드만 ultrawork 실행 |
| `.gitignore` | 누구든 수정 가능 (충돌 적음) |

---

## 상태 파일 형식

### docs-state.json (Docs Node)

```json
{
  "role": "docs",
  "active": false,
  "last_active": "2026-02-04T17:00:00Z",
  "current_task": null,
  "files_modified": []
}
```

### sync-state.json (Sync Node)

```json
{
  "role": "sync",
  "active": false,
  "last_active": "2026-02-04T17:00:00Z",
  "current_task": null,
  "last_sync": {
    "gmail": "2026-02-04T16:00:00Z",
    "slack": "2026-02-04T16:00:00Z"
  }
}
```

---

## 충돌 발생 시

1. **git status 확인** - 양쪽 변경사항 확인
2. **담당 노드 우선** - 해당 영역 담당 노드의 변경 우선
3. **수동 머지** - 필요 시 사용자가 직접 조율

---

## 커밋 컨벤션

노드 식별을 위한 커밋 메시지 접두사:

```
[docs] feat: PRD-0002 용어 통일
[sync] fix: Gmail 동기화 인코딩 오류 수정
```

---

## 빠른 참조

```
┌─────────────────────────────────────────────────────────────────┐
│                    노드 역할 빠른 참조                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  📝 Docs Node                    ⚙️ Sync Node                  │
│  ─────────────                   ────────────                   │
│  docs/prds/                      scripts/                       │
│  docs/strategies/                docs/management/               │
│  docs/mockups/                   .slacklist_state.json          │
│  .omc/plans/                     .sync_state.json               │
│  CLAUDE.md                       lists_sync.py                  │
│                                                                 │
│                    ⚠️ 공유 (조율 필요)                          │
│                    ───────────────────                          │
│                    .omc/ralplan-state.json                      │
│                    .omc/ultrawork-state.json                    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```
