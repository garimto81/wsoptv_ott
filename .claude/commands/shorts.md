---
name: shorts
description: PocketBase 사진으로 쇼츠 영상 생성 (Claude Vision 이미지 분석)
---

# /shorts - 쇼츠 영상 생성

PocketBase에서 사진을 가져와 마케팅용 쇼츠 영상을 생성합니다.
Claude Read 도구로 이미지를 직접 분석하여 자막과 Phase를 생성합니다.

## 사용법

```bash
/shorts list                          # 그룹 목록 조회
/shorts list -g <group_id>            # 그룹별 사진 조회
/shorts create -g <group_id> --auto   # 영상 생성 (CLI 호출)
/shorts batch -g <group_id>           # 전체 워크플로우 (권장)
```

## 서브커맨드

### list

그룹 및 사진 목록을 조회합니다.

**실행 방법:**
```bash
# 그룹 목록
node src/index.js groups

# 특정 그룹 사진
node src/index.js list -g <group_id>
```

### create

기존 CLI를 호출하여 영상을 생성합니다.

**실행 방법:**
```bash
node src/index.js create -g <group_id> --auto -n 50
```

**주요 옵션:**
- `--auto`: 대화형 프롬프트 생략
- `-n 50`: 그룹 전체 사진 사용 (기본 5장)
- `--tts`: TTS 음성 생성
- `--tts-voice sunhi`: 음성 선택

### batch (권장)

Claude Vision을 사용한 전체 자동화 워크플로우입니다.

**워크플로우:**

1. **Phase 1: 사진 다운로드**
   ```bash
   node .claude/skills/shorts-generator/scripts/download-photos.js <group_id> --limit=50
   ```
   - PocketBase에서 사진 조회
   - temp/ 디렉토리에 다운로드
   - JSON 형식으로 파일 목록 반환

2. **Phase 2: 이미지 분석 (Claude Read)**
   - temp/*.jpg 파일들을 Read 도구로 열어 분석
   - 각 이미지에 대해 Phase와 자막 생성

   **분석 프롬프트:**
   ```
   이 휠 복원 작업 이미지를 분석하세요.

   JSON 형식으로 반환:
   {
     "phase": "overview|before|process|after",
     "subtitle": "15자 이내 마케팅 문구",
     "confidence": 0.0-1.0
   }
   ```

3. **Phase 3: 결과 저장**
   ```bash
   node .claude/skills/shorts-generator/scripts/save-subtitles.js --input='<JSON>'
   ```
   - 분석 결과를 output/subtitles.json으로 저장

4. **Phase 4: 영상 생성**
   ```bash
   node src/index.js create -g <group_id> --auto -n 50 --subtitle-json ./output/subtitles.json --tts
   ```
   - FFmpeg로 영상 생성
   - Claude Vision 자막 적용
   - TTS 음성 포함

5. **Phase 5: 결과 보고**
   - 파일 경로, 길이, 품질 정보 출력

## Phase 분류 기준

| Phase | 시각적 특징 | 예시 |
|-------|------------|------|
| **overview** | 차량 전체 모습, 작업장 전경 | 차량 정면/후면, 리프트 위 |
| **before** | 손상/오염 상태, 스크래치 | 브레이크 더스트, 연석 자국 |
| **process** | 작업 중, 세척, 도장, 연마 | 물 분사, 도장 부스 |
| **after** | 복원 완료, 깨끗한 상태 | 신차급 광택, 완성된 휠 |

## 자막 작성 원칙

1. **15자 이내** - 짧고 임팩트 있게
2. **한글 사용** - 외래어/영어 최소화
3. **이미지 상태 반영** - before는 손상, after는 완성
4. **마케팅 톤** - 전문성과 신뢰감 전달
5. **다양성 유지** - 동일 패턴 반복 금지

## 예상 출력

```
🎬 Shorts 생성 워크플로우 시작

📥 Phase 1: 사진 다운로드
   ✓ 15개 이미지 다운로드 완료 (temp/)

🔍 Phase 2: Claude Vision 분석
   [1/15] l7v0rfmav504yup.jpg
         Phase: after (0.95)
         자막: "6시간의 정성이 담긴 결과"
   [2/15] 49zhdkcixcjmgos.jpg
         Phase: before (0.90)
         자막: "브레이크 더스트와 스크래치 가득"
   ...
   ✓ 15개 이미지 분석 완료

💾 Phase 3: 결과 저장
   ✓ output/subtitles.json 저장됨

🎬 Phase 4: 영상 생성
   ✓ 영상 생성 완료

📊 Phase 5: 결과 보고
   📁 출력: output/shorts_2026-01-09T12-00-00.mp4
   📐 해상도: 1080x1920
   ⏱️ 길이: 52초
```

## 관련 파일

| 파일 | 설명 |
|------|------|
| `src/index.js` | CLI 진입점 |
| `.claude/skills/shorts-generator/SKILL.md` | Skill 정의 |
| `.claude/skills/shorts-generator/scripts/` | 래퍼 스크립트 |
