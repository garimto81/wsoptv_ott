# PRD-0006-v4: WSOPTV Advanced Mode - 4계층 구조

| 항목 | 값 |
|------|---|
| **Version** | 4.0 |
| **Status** | Draft |
| **Priority** | P0 |
| **Created** | 2026-01-21 |
| **Author** | Claude Code |
| **Supersedes** | PRD-0006-v3 (3계층 구조) |

---

## Executive Summary

WSOPTV Advanced Mode의 **4계층 구조 설계**로, 멀티 뷰(Multi-View)와 멀티 스크린(PlayerCAM)을 **모두 수용**하는 통합 아키텍처를 제안합니다.

### 핵심 변경

| 버전 | 구조 | 특징 |
|------|------|------|
| v3 (기존) | 3계층 | PGM → Feature Table → PlayerCAM |
| **v4 (신규)** | **4계층** | **Event Strip → PGM → Feature Table → PlayerCAM** |

### 4계층 구조 개요

```
┌─────────────────────────────────────────────────────────────────────┐
│  Layer 0: Event Strip (이벤트 선택)                                   │
│           └── 최상단 가로 스크롤 - 어떤 대회인지 선택 (NBA TV와 동일)    │
├─────────────────────────────────────────────────────────────────────┤
│  Layer 1: PGM (Main Program)                                        │
│           └── 메인 영역 - 선택한 이벤트의 PD 연출 화면                  │
├─────────────────────────────────────────────────────────────────────┤
│  Layer 2: Feature Tables (Multi-View)                               │
│           └── 우측 상단 - 같은 이벤트 내 다른 테이블들                  │
├─────────────────────────────────────────────────────────────────────┤
│  Layer 3: PlayerCAM (Multi-Screen)                                  │
│           └── 우측 하단 - 선택한 테이블의 플레이어별 직캠               │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 용어 정의

### Multi-View vs Multi-Screen (PlayerCAM)

| 용어 | 정의 | 예시 | WSOPTV 적용 |
|------|------|------|-------------|
| **Multi-View** | 여러 **다른 테이블/대회**를 동시 시청 | NBA TV 4분할 | Layer 2 (Feature Tables) |
| **Multi-Screen** | 같은 테이블 내 **다른 앵글/선수** 동시 시청 | F1 Driver Cam | Layer 3 (PlayerCAM) |

> **결론**: 4계층 구조는 **Multi-View (Layer 2)**와 **Multi-Screen (Layer 3)**을 **동시에 제공**

---

## 4계층 구조 상세 설계

### 전체 레이아웃

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [Layer 0] EVENT STRIP                                                       │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                     │
│  │ 🔴 Main │ │ High   │ │ $1K    │ │ Super  │ │ Ladies │  ◀─ 이벤트 선택    │
│  │ Event  │ │ Roller │ │ Daily  │ │ Circuit│ │ Event  │                     │
│  │ Day 7  │ │ FT     │ │ Day 2  │ │ #12    │ │ Day 1  │                     │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────┐ ┌────────────────────┐ │
│  │                                                 │ │ [Layer 2] TABLES   │ │
│  │                                                 │ │ ┌──────────────┐   │ │
│  │               [Layer 1] PGM                     │ │ │ 🟢 Table 1   │   │ │
│  │            Main Event Final Table               │ │ │ POT: $2.4M   │   │ │
│  │                                                 │ │ │ [CAM 지원]   │   │ │
│  │              🔴 LIVE  POT: $4.2M                │ │ └──────────────┘   │ │
│  │                                                 │ │ ┌──────────────┐   │ │
│  │                                                 │ │ │ 🟡 Table 2   │   │ │
│  │                                                 │ │ │ POT: $890K   │   │ │
│  │                                                 │ │ └──────────────┘   │ │
│  │                                                 │ │ ┌──────────────┐   │ │
│  │                                                 │ │ │ 🟢 Table 3   │   │ │
│  │                                                 │ │ │ POT: $1.1M   │   │ │
│  │                                                 │ │ │ [CAM 지원]   │   │ │
│  │                                                 │ │ └──────────────┘   │ │
│  └─────────────────────────────────────────────────┘ ├────────────────────┤ │
│                                                      │ [Layer 3] PLAYER   │ │
│                                                      │ CAM (테이블 선택 시)│ │
│                                                      │ ┌──────┐ ┌──────┐  │ │
│                                                      │ │Ivey  │ │Dwan  │  │ │
│                                                      │ └──────┘ └──────┘  │ │
│                                                      │ ┌──────┐ ┌──────┐  │ │
│                                                      │ │Polk  │ │Perkins│ │ │
│                                                      │ └──────┘ └──────┘  │ │
│                                                      └────────────────────┘ │
├─────────────────────────────────────────────────────────────────────────────┤
│  [Controls] ▶ Pause  |  🔊 Volume  |  [Tables] [MultiView] [Key Hands]     │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### Layer 0: Event Strip (이벤트 선택)

> **NBA TV 참조**: Score Strip - 상단 가로 스크롤로 모든 경기 점수 실시간 표시

#### 개념

**최상단에 고정된 가로 스크롤 바**로, 현재 진행 중인 모든 **이벤트(대회)**를 표시합니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  [Main Event] [High Roller] [$1K Daily] [Super Circuit] [Ladies] [Seniors]  │
│    🔴 Day 7     🔴 FT         Day 2        #12           Day 1     Day 3   │
│    24 Tables    9 Players     180 Left     Heads Up      312 Left  45 Left │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 기능 요구사항

| ID | 요구사항 | 설명 | 우선순위 |
|----|---------|------|:--------:|
| ES-1.1 | 이벤트 목록 표시 | 진행 중 대회 가로 스크롤 | P0 |
| ES-1.2 | LIVE 인디케이터 | 라이브 진행 중 배지 | P0 |
| ES-1.3 | 상태 정보 | 진행 단계 (Day N, FT, Heads Up 등) | P0 |
| ES-1.4 | 참가자 정보 | 남은 인원 / 테이블 수 | P1 |
| ES-1.5 | 클릭 선택 | 이벤트 클릭 시 해당 이벤트로 전환 | P0 |

#### NBA TV 매핑

| NBA TV 요소 | WSOPTV 대응 | 비고 |
|------------|-------------|------|
| Score Strip | Event Strip | 상단 가로 스크롤 |
| 팀 로고 | 이벤트 아이콘/뱃지 | Main Event, High Roller 등 |
| 점수 (e.g. 84-53) | 남은 인원 / 테이블 수 | 24 Tables, 9 Players |
| 쿼터 (Q3 5:23) | 진행 단계 | Day 7, Final Table |

---

### Layer 1: PGM (Main Program)

#### 개념

**메인 영역에 표시되는 PD 연출 화면**으로, 선택한 이벤트의 대표 테이블 방송입니다.

#### 특징

| 항목 | 설명 |
|------|------|
| **위치** | 화면 좌측 대부분 (약 70%) |
| **콘텐츠** | 한국 스튜디오 종편 PGM |
| **해설** | 기본 해설 트랙 |
| **전환** | Event Strip에서 이벤트 선택 시 변경 |

#### 기능 요구사항

| ID | 요구사항 | 설명 | 우선순위 |
|----|---------|------|:--------:|
| PGM-1.1 | 메인 PGM 표시 | 선택 이벤트의 대표 방송 | P0 |
| PGM-1.2 | 실시간 POT 오버레이 | 현재 팟 크기 표시 | P0 |
| PGM-1.3 | LIVE 배지 | 우측 상단 라이브 인디케이터 | P1 |
| PGM-1.4 | 풀스크린 전환 | 더블 클릭 시 풀스크린 | P1 |

---

### Layer 2: Feature Tables (Multi-View)

> **Multi-View 기능**: 같은 이벤트 내 **여러 다른 테이블**을 동시 표시

#### 개념

**우측 상단에 배치된 테이블 목록**으로, 선택한 이벤트 내의 다른 테이블들을 보여줍니다.

#### 레이아웃

```
┌────────────────────┐
│  FEATURE TABLES    │
├────────────────────┤
│ ┌──────────────┐   │
│ │ 🟢 Table 1   │   │  ◀─ 클릭 시 PGM으로 전환
│ │ POT: $2.4M   │   │     또는 PlayerCAM 활성화
│ │ [CAM 지원]   │   │  ◀─ PlayerCAM 가능 테이블 표시
│ └──────────────┘   │
│ ┌──────────────┐   │
│ │ 🟡 Table 2   │   │  ◀─ CAM 미지원 테이블
│ │ POT: $890K   │   │
│ └──────────────┘   │
│ ┌──────────────┐   │
│ │ 🟢 Table 3   │   │
│ │ POT: $1.1M   │   │
│ │ [CAM 지원]   │   │
│ └──────────────┘   │
│ ┌──────────────┐   │
│ │ + Add Table  │   │  ◀─ 빈 슬롯 가이드
│ │              │   │
│ └──────────────┘   │
└────────────────────┘
```

#### 기능 요구사항

| ID | 요구사항 | 설명 | 우선순위 |
|----|---------|------|:--------:|
| FT-1.1 | 테이블 목록 표시 | 이벤트 내 진행 중 테이블 목록 | P0 |
| FT-1.2 | 실시간 POT 표시 | 각 테이블 현재 팟 크기 | P0 |
| FT-1.3 | CAM 지원 뱃지 | PlayerCAM 가능 테이블 표시 | P0 |
| FT-1.4 | 테이블 클릭 | PGM 전환 또는 PlayerCAM 활성화 | P0 |
| FT-1.5 | 테이블 추가 | "+ Add Table" 가이드 (NBA 스타일) | P1 |

#### 테이블 상태 표시

| 상태 | 아이콘 | 설명 |
|------|:------:|------|
| CAM 지원 + 라이브 | 🟢 | PlayerCAM 가능, 실시간 방송 |
| 라이브 only | 🟡 | PlayerCAM 미지원, 실시간 방송 |
| Timeshift | 🔵 | 다시보기 가능 |

---

### Layer 3: PlayerCAM (Multi-Screen)

> **Multi-Screen 기능**: 선택한 테이블 내 **각 플레이어의 개별 직캠**

#### 개념

**우측 하단에 배치된 플레이어별 카메라 피드**로, Layer 2에서 CAM 지원 테이블을 선택했을 때만 활성화됩니다.

#### 레이아웃

```
┌────────────────────┐
│  PLAYER CAM        │
│  Table 1 - FT      │
├────────────────────┤
│ ┌───────┐ ┌───────┐│
│ │ Ivey  │ │ Dwan  ││  ◀─ 클릭 시 확대
│ │ 🔊    │ │       ││  ◀─ 오디오 선택
│ └───────┘ └───────┘│
│ ┌───────┐ ┌───────┐│
│ │ Polk  │ │Perkins││
│ │       │ │       ││
│ └───────┘ └───────┘│
│ ┌───────┐ ┌───────┐│
│ │Hellmut│ │Negreanu│
│ │       │ │       ││
│ └───────┘ └───────┘│
└────────────────────┘
```

#### 기능 요구사항

| ID | 요구사항 | 설명 | 우선순위 |
|----|---------|------|:--------:|
| PC-1.1 | PlayerCAM 그리드 | 테이블 내 플레이어별 직캠 표시 | P1 |
| PC-1.2 | 플레이어명 표시 | 각 캠에 플레이어 이름 오버레이 | P1 |
| PC-1.3 | 오디오 선택 | 한 플레이어만 오디오 활성화 | P1 |
| PC-1.4 | 클릭 확대 | 특정 PlayerCAM 클릭 시 PGM 영역으로 확대 | P1 |
| PC-1.5 | 비활성 상태 | CAM 미지원 테이블 선택 시 안내 메시지 | P1 |

#### 비활성 상태 표시

```
┌────────────────────┐
│  PLAYER CAM        │
├────────────────────┤
│                    │
│  ┌──────────────┐  │
│  │              │  │
│  │  PlayerCAM   │  │
│  │  지원 테이블  │  │
│  │  선택 시     │  │
│  │  활성화      │  │
│  │              │  │
│  └──────────────┘  │
│                    │
└────────────────────┘
```

---

## 인터랙션 시나리오

### 시나리오 1: 이벤트 간 전환 (Multi-View)

```
1. 사용자가 Event Strip에서 "High Roller" 클릭
2. Layer 1 (PGM)이 High Roller Final Table로 전환
3. Layer 2 (Feature Tables)가 High Roller의 테이블 목록으로 업데이트
4. Layer 3 (PlayerCAM)이 초기화 (새 테이블 선택 대기)
```

### 시나리오 2: 테이블 내 PlayerCAM 선택 (Multi-Screen)

```
1. Layer 2에서 "[CAM 지원] Table 1" 클릭
2. Layer 1 (PGM)이 Table 1 피드로 전환
3. Layer 3 (PlayerCAM)이 Table 1의 플레이어 직캠 그리드 표시
4. 사용자가 "Phil Ivey" 캠 클릭
5. Phil Ivey 캠이 PGM 영역으로 확대 (Picture-in-Picture 또는 전체 전환)
```

### 시나리오 3: 4분할 Multi-View 모드

```
1. 하단 컨트롤에서 "MultiView" 버튼 클릭
2. PGM 영역이 4분할 그리드로 전환
3. 각 슬롯에 다른 이벤트 또는 테이블 배치 가능
4. Event Strip 또는 Feature Tables에서 드래그 앤 드롭으로 슬롯 채움
```

---

## 4계층 vs 3계층 비교

| 항목 | 3계층 (기존) | 4계층 (신규) |
|------|-------------|-------------|
| **이벤트 선택** | 별도 페이지/메뉴 | **Layer 0 (Event Strip)** |
| **Multi-View** | Feature Table만 | Event Strip + Feature Table |
| **Multi-Screen** | PlayerCAM | PlayerCAM (동일) |
| **NBA TV 호환성** | 부분 | **완전 호환** |
| **워크플로우** | 이벤트 → 페이지 전환 | **화면 내 전환** |

### 장점

1. **NBA TV UX 완전 호환**: Score Strip 패턴 1:1 매핑
2. **빠른 이벤트 전환**: 페이지 이동 없이 화면 내 전환
3. **Multi-View와 Multi-Screen 동시 지원**: 이벤트 간 + 테이블 내 모두 커버
4. **확장성**: 향후 더 많은 이벤트/테이블 추가 용이

---

## 레이아웃 변형

### 모드 1: Standard View (기본)

```
┌───────────────────────────────────────────────────────┐
│  [Event Strip]                                         │
├───────────────────────────────────────────────────────┤
│  ┌─────────────────────────────┐ ┌─────────────────┐  │
│  │                             │ │  Feature Tables │  │
│  │           PGM               │ ├─────────────────┤  │
│  │                             │ │  PlayerCAM      │  │
│  └─────────────────────────────┘ └─────────────────┘  │
└───────────────────────────────────────────────────────┘
```

### 모드 2: Multi-View 4분할

```
┌───────────────────────────────────────────────────────┐
│  [Event Strip]                                         │
├───────────────────────────────────────────────────────┤
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────────┐  │
│  │ Event 1    │ │ Event 2    │ │                  │  │
│  │ PGM        │ │ PGM        │ │  Feature Tables  │  │
│  ├─────────────┤ ├─────────────┤ │  (선택 이벤트)   │  │
│  │ Event 3    │ │ Event 4    │ ├─────────────────┤  │
│  │ PGM        │ │ Add Event  │ │  PlayerCAM      │  │
│  └─────────────┘ └─────────────┘ └─────────────────┘  │
└───────────────────────────────────────────────────────┘
```

### 모드 3: PlayerCAM Focus

```
┌───────────────────────────────────────────────────────┐
│  [Event Strip]                                         │
├───────────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────────────┐│
│  │                                                   ││
│  │              Selected PlayerCAM (확대)            ││
│  │                    Phil Ivey                      ││
│  │                                                   ││
│  ├─────────────────────────────────────────────────┤│
│  │ [PGM] [Table] [Ivey] [Dwan] [Polk] [Perkins]    ││
│  └───────────────────────────────────────────────────┘│
└───────────────────────────────────────────────────────┘
```

---

## 기술 요구사항

### 데이터 구조

```typescript
interface EventStrip {
  events: Event[];
  selectedEventId: string;
}

interface Event {
  id: string;
  name: string;           // "Main Event", "High Roller"
  status: 'live' | 'upcoming' | 'completed';
  stage: string;          // "Day 7", "Final Table", "Heads Up"
  remainingPlayers: number;
  remainingTables: number;
  tables: Table[];
}

interface Table {
  id: string;
  name: string;           // "Table 1", "Feature Table"
  pot: number;
  blinds: string;         // "50K/100K"
  hasPlayerCam: boolean;  // PlayerCAM 지원 여부
  players: Player[];
  streamUrl: string;
}

interface Player {
  id: string;
  name: string;
  chips: number;
  position: string;       // "BTN", "SB", "BB"
  camStreamUrl?: string;  // PlayerCAM URL (optional)
}
```

### API 엔드포인트

| 엔드포인트 | 설명 | 반환 |
|-----------|------|------|
| `GET /events` | 진행 중 이벤트 목록 | `Event[]` |
| `GET /events/:id/tables` | 이벤트 내 테이블 목록 | `Table[]` |
| `GET /tables/:id/players` | 테이블 내 플레이어 | `Player[]` |
| `GET /tables/:id/stream` | 테이블 PGM 스트림 | `StreamUrl` |
| `GET /players/:id/cam` | PlayerCAM 스트림 | `StreamUrl` |

---

## 구현 우선순위

### Phase 1 (MVP)

| 기능 | 설명 | 우선순위 |
|------|------|:--------:|
| Event Strip | 이벤트 목록 + 선택 | P0 |
| PGM | 메인 방송 표시 | P0 |
| Feature Tables | 테이블 목록 (CAM 뱃지) | P0 |
| PlayerCAM Grid | 플레이어 직캠 그리드 | P1 |

### Phase 2

| 기능 | 설명 | 우선순위 |
|------|------|:--------:|
| 4분할 Multi-View | 여러 이벤트 동시 표시 | P1 |
| PlayerCAM Focus | 직캠 확대 모드 | P1 |
| Key Hands | 주요 핸드 점프 | P1 |

### Phase 3

| 기능 | 설명 | 우선순위 |
|------|------|:--------:|
| StatsView 통합 | HUD 오버레이 | P2 |
| AI Key Hands | 자동 하이라이트 | P2 |
| Position Analysis | 포지션 분석 시각화 | P2 |

---

## UI 목업 요청

### 필요 목업 목록

| # | 목업 | 파일명 | 설명 |
|:-:|------|--------|------|
| 1 | 4계층 Standard View | `4layer-standard.html` | 기본 레이아웃 |
| 2 | 4계층 Multi-View | `4layer-multiview.html` | 4분할 모드 |
| 3 | 4계층 PlayerCAM Focus | `4layer-playercam-focus.html` | 직캠 확대 모드 |
| 4 | Event Strip 상세 | `event-strip.html` | Event Strip 단독 |

---

## References

- [PRD-0006 v3 (3계층)](PRD-0006-advanced-mode.md) - 이전 버전
- [ADR-0001 3계층 설계 근거](../adrs/ADR-0001-multiview-3layer-rationale.md)
- [REPORT NBA TV 분석](../reports/REPORT-2026-01-19-nbatv-reference-analysis.md)
- [REPORT 프로덕션 이슈](../reports/REPORT-2026-01-21-multiview-production-issues.md)

---

*Created: 2026-01-21*
*Author: Claude Code*
